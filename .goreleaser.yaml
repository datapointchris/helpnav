version: 2

builds:
  - main: .
    binary: helpnav
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/datapointchris/helpnav/cmd.version={{.Version}}
      - -X github.com/datapointchris/helpnav/cmd.commit={{.ShortCommit}}
      - -X github.com/datapointchris/helpnav/cmd.date={{.Date}}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: "helpnav_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

changelog:
  sort: asc
